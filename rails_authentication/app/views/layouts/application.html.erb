<!DOCTYPE html>
<html>
  <head>
    <title>Amazon43</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body>
    <a href="/">Home</a>
    |
    <a href="/about">About</a>
    |
    <a href="/contact">Contact</a>
    |
    <a href="/new_bill">New Bill</a>
    |
    <%= link_to "Products", products_path %>
    |
    <%= link_to "New Product", new_product_path %>
    |
    <%= link_to "log in", new_session_path %>|
    |
    <%= link_to "sign up", new_user_path %>
    |
    <% if user_signed_in? %>
      <% link_to "log out", session_path, method: :delete, data:{confirm:"are you sure you want to log out?"} %>
      |
      <%= link_to "Logout", session_path, method: :delete, data: {confirm: "are you sure you want to log out?"}%>
      <div>
        <h2>Hello <%= current_user.full_name %></h2>
        <% else %>
      </div>
    <% end %>
    <div>
      <% flash.each do |type, message| %>
        <%= type + "-------" + message %>
      <% end %>
    </div>
    <%= yield %>
  </body>
</html>
